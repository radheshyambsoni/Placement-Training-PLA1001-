// import java.util.Arrays;

public class May21_maxEqbInArr{
    static int x,y;
    public static void main(String[] args) {
        // Maximum Equilibirium sum in an array
        // int arr[]={5,1,-5,-1,2,6,-4,2};
        // int i=0,j=arr.length-1,count=1,maxSum=Integer.MIN_VALUE;
        // int prefixSum=arr[i],suffixSum=arr[j];
        // while(count<arr.length){
        //     count++;
        //     if(prefixSum>suffixSum){
        //         suffixSum+=arr[--j];
        //     }else if(prefixSum<suffixSum){
        //         prefixSum+=arr[++i];
        //     }else if(prefixSum==suffixSum){
        //         maxSum=Math.max(maxSum,prefixSum);
        //         x=i;y=j;
        //     }else{
        //         maxSum=-1;
        //     }
        // }
        // while(i!=j){
        //     prefixSum+=arr[i];
        //     suffixSum+=arr[i];
        //     maxSum=Math.max(maxSum,prefixSum);
        //     x=++i;
        //     y--;
        // }

        // int prefixArray[]=new int[x];
        // for(i=0;i<x;i++){
        //     prefixArray[i]=arr[i];
        // }
        // int sufffixArray[]=new int[arr.length-y];
        // for(i=0,j=y;i<sufffixArray.length;i++,j++){
        //     prefixArray[i]=arr[j];
        // }
        // if(maxSum==-1){
        //     System.out.println("No maximum equilibirium sum possible for the array");
        // }else{
        //     System.out.println(x+" "+y);
        //     System.out.println("Maximum Equilibirium sum: "+maxSum);
        //     // System.out.println("Prefix Array: "+Arrays.toString(prefixArray));
        //     // System.out.println("Prefix Array: "+Arrays.toString(sufffixArray));
        // }

        // int leftSum=0,Sum=0;
        // for(int i=0;i<arr.length;i++){Sum+=arr[i];}
        // int x;
        // for(int i=0;i<arr.length;i++){
            
        // }
    }
}